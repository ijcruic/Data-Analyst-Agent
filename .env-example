# Environment Configuration for Agentic AI Data Analyst
# Copy this file to .env and fill in your values

# OpenAI API Configuration (Required)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: LLM Model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: Agent Temperature (default: 0.7, range: 0.0-2.0)
# Lower = more deterministic, Higher = more creative
# AGENT_TEMPERATURE=0.7

# Optional: MCP server URLs (default: http://mcp-server:4242/mcp inside Docker)
# Comma-separated list; the agent will try each in order until one succeeds.
# Example:
# MCP_URL=http://mcp-server:4242/mcp,http://backup-mcp:4242/mcp
